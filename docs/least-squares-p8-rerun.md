# Least-squares (p=8) rerun

## 目的

将论文中四组固定 (k=7)、考察字典规模 (N) 的实验由辅助样条次数 (p=7) 改为 (p=8)，使二维和三维实验都满足

\[
p+1\ge s_{\mathrm{cap}}(d)=\frac{d+2k+1}{2}.
\]

本文件只记录后续重跑任务；当前论文中的脚本、数据、图片和数值表保持不变。

## 需要重跑的实验

- 二维线弹性关于 (N) 的收敛实验；
- 平面应力关于 (N) 的收敛实验；
- 三维线弹性关于 (N) 的收敛实验；
- 二维 Kirchhoff--Love 板弯曲关于 (N) 的收敛实验。

## 执行清单

- [ ] 将上述四组配置中的辅助 B 样条次数统一改为 (p=8)。
- [ ] 对每个 (N) 重新确定各坐标方向的样条自由度，并记录新的总维数 (K)；不要沿用 (p=7) 的 (K)。
- [ ] 根据新的 (K) 与杠杆包络重新选择 (Q_{\mathrm R})，至少检查谱稳定条件
  \[
  Q_{\mathrm R}\gtrsim C_{\mathrm R,K}^{(m)}\log(2K/\eta).
  \]
- [ ] 保持训练样本与 Ritz 投影样本独立，继续使用与正文一致的重复次数、验证规则、测试规则和系数预算选择流程。
- [ ] 重跑全部随机重复并保存原始误差、均值和样本标准差。
- [ ] 重新生成四幅关于 (N) 的收敛图，不覆盖旧结果，直至新结果通过检查。
- [ ] 重新拟合并报告应力/弯矩与位移/挠度的观测阶。
- [ ] 更新论文中的四张配置表、四张数值表、四幅图片及相邻文字说明。
- [ ] 重跑 `pnpm build`，检查公式、图表、交叉引用和参考文献。

## 验收重点

- 配置表中的 (p)、(K) 和 (Q_{\mathrm R}) 与实际运行记录一致。
- 新的 (p=8) 实验满足 degree-match，但图中理论斜率仍应准确说明其所代表的误差分量。
- 不以单次随机结果报告收敛阶；正文继续使用全部独立重复的统计量。
- 在确认新图表和观测阶无误前，保留当前 (p=7) 结果以便复核。
