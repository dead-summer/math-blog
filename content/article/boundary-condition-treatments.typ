#import "/typ/templates/blog.typ": *

#show: main-zh.with(
  title: "本质边界条件的三种处理：投影、泛函残差与边界截断",
  author: "summer",
  desc: [比较混合最小二乘离散中正交投影、边界泛函残差与边界截断函数对残差等价性、Sobolev 逼近率和经验泛化估计的影响],
  date: "2026-09-01",
  tags: (
    blog-tags.numerical-methods,
    blog-tags.pde,
  ),
  show-outline: true,
)

= 摘要

混合最小二乘方法必须同时处理内部方程和本质边界条件。对线弹性位移与固支板挠度，常见做法有三种：先在无约束近似空间中逼近，再投影到齐次边界空间；把边界条件加入最小二乘泛函；在试探函数中乘固定截断函数，从构造上强制满足边界条件。

这三种做法在计算上看似可以互换，在分析上却不等价。本文得到以下确定结论：

+ 正交投影完整保留受约束空间上的连续残差等价性，并在相同 Sobolev 正则性假设下无损传递原有逼近率。有限维实现只会额外引入投影离散误差和积分误差。
+ 把普通 $L^2(partial Omega)$ 边界残差加入泛函，不能得到与 $H^1$ 或 $H^2$ 图范数一致的双边等价；任意固定罚参数都不能修复这一问题。只有采用自然迹空间的商范数，连续残差等价性才成立。
+ 固定边界截断函数不改变受约束空间上的连续残差等价性，但改变了近似类。原问题解的 Sobolev 正则性不足以推出相同逼近率；要恢复原速率，必须对除以截断函数后的商函数另作同阶正则性假设。

因此，如果目标是在不改变现有连续稳定性、逼近假设和误差分解的前提下处理边界条件，正交投影是三者中唯一可以直接使用的方案。自然迹残差在连续层面同样正确，但需要重新构造边界项的离散与经验误差分析；边界截断只有在明确验证商函数正则性后才能替代投影。

= 统一问题与判定标准

设 $m=1$ 表示线弹性位移，$m=2$ 表示 Kirchhoff--Love 固支板挠度。记
$
  V := H^m(Omega),
  quad V_0 := H_0^m(Omega),
$
向量值情形逐分量理解。应力或弯矩所在的图空间统一记为 $Sigma$，相应图范数记为 $norm(dot)_Sigma$。设内部一阶系统的残差算子为
$
  cal(R): Sigma times V -> cal(Y),
$
并假设在正确施加本质边界条件的空间 $Sigma times V_0$ 上已经证明
$
  c_0 (norm(bold(tau))_Sigma^2 + norm(v)_(H^m)^2)
  <= norm(cal(R)(bold(tau),v))_cal(Y)^2
  <= C_0 (norm(bold(tau))_Sigma^2 + norm(v)_(H^m)^2).
$
这里 $c_0,C_0>0$ 与字典规模、采样数及其他离散参数无关。线弹性中 $cal(R)$ 包含本构残差与平衡残差；板弯曲中包含弯矩--曲率残差与双散度平衡残差。

评价一种边界处理方式，必须分别检查三个彼此独立的问题：

1. *连续稳定性*：新泛函是否仍与目标图范数平方一致双边等价，并且等价常数不随离散参数退化；
2. *确定性逼近*：原有字典逼近定理能否在不增强精确解正则性的条件下传递到满足边界条件的试探函数；
3. *训练误差*：新增边界操作能否被现有的数值积分、Rademacher 复杂度或矩阵扰动估计覆盖。

只验证试探函数满足边界条件，并不足以回答后两个问题。

= 正交投影

== 连续投影及其非扩张性

令 $P_m:V -> V_0$ 为 $H^m(Omega)$ 内积下到闭子空间 $V_0$ 的正交投影，即
$
  (P_m v,z)_(H^m) = (v,z)_(H^m)
  quad "for every" z in V_0.
$
它满足
$
  P_m^2=P_m,
  quad norm(P_m v)_(H^m) <= norm(v)_(H^m),
  quad norm((I-P_m)v)_(H^m) = inf_(z in V_0) norm(v-z)_(H^m).
$

#proposition(title: [投影无损传递逼近误差])[
  设精确物理解 $v^star in V_0$，$v_N in V$ 是任意无约束近似，则
  $
    norm(v^star-P_m v_N)_(H^m)
    <= norm(v^star-v_N)_(H^m).
  $
]

#proof[
  由 $P_m v^star=v^star$ 以及正交投影的非扩张性，
  $
    norm(v^star-P_m v_N)_(H^m)
    =norm(P_m(v^star-v_N))_(H^m)
    <=norm(v^star-v_N)_(H^m).
  $
]

因此，如果无约束字典给出
$
  inf_(v_N in V_N) norm(v^star-v_N)_(H^m)
  <= C N^(-beta) norm(v^star)_(H^s),
$
那么投影像空间 $P_m V_N subset V_0$ 自动满足
$
  inf_(z_N in P_m V_N) norm(v^star-z_N)_(H^m)
  <= C N^(-beta) norm(v^star)_(H^s).
$
指数、正则性阶数和右端的解范数都不改变。由于 $(bold(tau)_N,P_m v_N)$ 已属于 $Sigma times V_0$，原来的残差等价定理也可以直接应用。连续稳定性证明和最佳逼近证明均不需要重写。

== 有限维投影实现的代价

实际计算通常用辅助空间 $W_K subset V_0$ 定义 $H^m$-Ritz 投影 $P_(m,K)$，并通过数值积分组装投影矩阵。此时计算的试探空间是
$
  P_(m,K) V_N subset W_K subset V_0,
$
而不是精确的 $P_m V_N$。由于 $P_(m,K)$ 仍是非扩张投影，对 $v^star in V_0$ 有
$
  norm(v^star-P_(m,K)v_N)_(H^m)
  <=norm((I-P_(m,K))v^star)_(H^m)
  +norm(v^star-v_N)_(H^m).
$
因此总误差中新增的是 $W_K$ 对精确物理解的 Ritz 截断误差，以及投影矩阵的积分扰动误差。若投影问题本身稳定，并且 $W_K$ 对精确解有一致逼近能力，这两项可随 $K$ 和投影积分点数增加而收敛。

有限维投影并不破坏连续残差等价性：每个 $P_(m,K)v_N$ 仍然属于 $V_0$。它改变的是离散逼近误差分解，并引入辅助维数 $K$。因此，对这一方案的准确表述是：

+ 精确投影无损保留残差等价性和原字典逼近率；
+ Ritz 投影保留残差等价性，但增加可单独估计的投影截断误差、投影积分误差和矩阵求解误差。

== 应力平均迹约束不是边界投影

线弹性纯位移问题还会使用平均应力迹归一化。它与位移边界迹是两个不同问题。定义
$
  P_"tr" bold(tau)
  := bold(tau)
  - 1/(d abs(Omega))
    integral_Omega tr(bold(tau)) dif x bold(I).
$
则 $integral_Omega tr(P_"tr" bold(tau)) dif x=0$，并且减去的常张量散度为零。该操作是秩一修正，计算代价很小，不需要边界积分，也不会增加散度误差。这个归一化应当继续显式实施，不能与下面讨论的边界迹罚项混为一谈。

= 将边界条件收入泛函残差

== 普通边界 $L^2$ 罚项不能控制图范数

最直接的想法是在内部泛函后增加
$
  alpha norm(gamma_0 v)_(L^2(partial Omega))^2,
$
板弯曲时再增加 $alpha_1 norm(gamma_1 v)_(L^2(partial Omega))^2$。这里 $gamma_0 v$ 是函数迹，$gamma_1 v$ 是法向导数迹，$alpha,alpha_1$ 是固定正数。

这一做法不能得到目标图范数的一致下界。失败原因不是罚参数不够大，而是 $L^2$ 边界范数比自然迹范数弱半阶以上，高频边界模态可以在图范数中增长、同时保持 $L^2$ 边界残差有界。

#proposition(title: [线弹性的高频反例])[
  取二维单位圆盘，令
  $
    phi_n(r,theta)=1/n r^n cos(n theta),
    quad bold(u)_n=nabla phi_n,
    quad bold(sigma)_n=2 mu nabla^2 phi_n.
  $
  则 $Delta phi_n=0$，从而本构残差、平衡残差以及平均应力迹都等于零。同时
  $
    norm(bold(u)_n)_(L^2(partial Omega))^2 tilde.eq 1,
  $
  而
  $
    norm(bold(sigma)_n)_(bold(H)(div))^2
    +norm(bold(u)_n)_(H^1)^2 tilde.eq n.
  $
  因而内部残差加任意固定权重的 $L^2(partial Omega)$ 位移罚项，都不可能一致控制线弹性图范数。
]

#proposition(title: [板弯曲的高频反例])[
  在同一圆盘上取
  $
    w_n=phi_n,
    quad bold(M)_n=bold(cal(C)) nabla^2 w_n,
  $
  其中 $bold(cal(C))$ 为常系数各向同性板刚度。由于 $Delta^2 w_n=0$，内部弯矩--曲率残差与平衡残差均为零，并且
  $
    norm(w_n)_(L^2(partial Omega))^2 lt.tilde 1,
    quad norm(partial_n w_n)_(L^2(partial Omega))^2 tilde.eq 1.
  $
  另一方面，
  $
    norm(bold(M)_n)_(bold(H)(div div))^2
    +norm(w_n)_(H^2)^2 tilde.eq n.
  $
  所以即使同时用固定权重惩罚 $w$ 与 $partial_n w$ 的边界 $L^2$ 残差，也不能得到与板图范数一致的强制性。
]

把罚参数取成依赖网格尺寸、特征频率或字典规模的量，只能借助离散逆估计得到离散空间上的控制。所得稳定常数必然依赖离散尺度，因而不是原有的连续、参数一致残差等价定理。由此可以直接排除以下论证：先用固定 $L^2$ 边界罚项替代投影，再声称原图范数等价性和原误差常数保持不变。这个声称是错误的。

普通边界 $L^2$ 罚项仍可在固定有限维空间上定义可计算的最小二乘目标，也可以在该泛函自身诱导的范数中作误差分析。代价是结论已经改变：它给出的不是目标 $H^1$ 或 $H^2$ 图范数的一致误差界；若再用逆估计换回图范数，常数将随离散尺度退化。

== 正确边界项是自然迹商范数

在 $V=H^m(Omega)$ 上定义商空间
$
  Y_m:=V/V_0,
$
并以等价类 $T_m v:=[v]$ 表示广义边界迹。它的标准商范数为
$
  norm(T_m v)_(Y_m)
  :=inf_(z in V_0) norm(v-z)_(H^m).
$
对 $m=1$，该范数等价于 $H^(1/2)(partial Omega)$ 中的函数迹范数。对光滑边界上的 $m=2$，它等价于迹对
$
  (gamma_0 v,gamma_1 v)
  in H^(3/2)(partial Omega) times H^(1/2)(partial Omega)
$
的自然范数；在多边形区域上还必须包含各边迹在顶点处的相容性，不能简单地把每条边的两个分数阶范数互相独立相加。

正交投影给出了这个商范数的最小能量延拓：
$
  norm(T_m v)_(Y_m)=norm((I-P_m)v)_(H^m).
$
因此，投影与自然迹残差在连续层面实际上是同一个空间分解的两种表达。

定义扩充泛函
$
  cal(J)_partial(bold(tau),v)
  :=norm(cal(R)(bold(tau),v))_cal(Y)^2
  +norm(T_m v)_(Y_m)^2.
$

#theorem(title: [自然迹残差恢复完整图范数等价])[
  若内部残差在 $Sigma times V_0$ 上满足开头给出的双边等价，并且 $cal(R)$ 关于 $v$ 在 $H^m$ 范数下连续，则存在与离散参数无关的 $c,C>0$，使
  $
    c (norm(bold(tau))_Sigma^2+norm(v)_(H^m)^2)
    <=cal(J)_partial(bold(tau),v)
    <=C (norm(bold(tau))_Sigma^2+norm(v)_(H^m)^2)
  $
  对所有 $(bold(tau),v) in Sigma times V$ 成立。
]

#proof[
  写成
  $
    v=P_m v+e,
    quad e=(I-P_m)v.
  $
  于是 $P_m v in V_0$ 且 $norm(e)_(H^m)=norm(T_m v)_(Y_m)$。由残差连续性，
  $
    norm(cal(R)(bold(tau),P_m v))_cal(Y)
    lt.tilde norm(cal(R)(bold(tau),v))_cal(Y)+norm(e)_(H^m).
  $
  对 $(bold(tau),P_m v)$ 使用受约束空间上的残差下界，再由
  $norm(v)_(H^m)<=norm(P_m v)_(H^m)+norm(e)_(H^m)$，得到所需下界。上界由 $cal(R)$ 的连续性以及迹商范数不超过 $H^m$ 范数直接得到。
]

这个定理说明：将边界条件收入泛函并非原则上不可行；不可行的是使用过弱的点态 $L^2$ 边界度量。采用自然迹商范数后，连续图范数等价性严格成立。

== 对逼近率与训练误差的影响

设精确解 $v^star in V_0$，则 $T_m v^star=0$。对任意无约束近似 $v_N$，
$
  norm(T_m(v_N-v^star))_(Y_m)
  <=norm(v_N-v^star)_(H^m).
$
所以原有 $H^m$ 字典逼近率足以控制自然迹残差；确定性最佳逼近指数无需改变，也不需要商函数假设。

但是，经验训练误差分析不能原样保留。原因是 $H^(1/2)$、$H^(3/2)$ 等分数阶迹范数是非局部量，不能写成独立边界采样点上残差平方的简单平均。采用自然迹残差后有两条严格可行的路线：

+ 对有限维迹空间精确组装分数阶迹 Gram 矩阵。此时边界项是确定性二次型，不产生 Monte Carlo 边界泛化误差，但需要分析该 Gram 矩阵的计算与条件数；
+ 对分数阶双重积分或等价边界算子作数值近似。此时必须新增非局部边界积分的离散误差和经验过程估计，原来只针对点态内部残差的泛化定理不能直接套用。

尤其值得注意的是，用定义式计算商范数需要求最小能量延拓，即计算 $(I-P_m)v$。这与求解全局正交投影本质相同。因此，自然迹残差在连续理论上很干净，却没有自动消除投影的全局计算代价。

= 用固定边界截断函数强制满足边界条件

== 构造及其连续稳定性

在盒状区域 $Omega=(0,1)^d$ 上定义
$
  b_1(x):=product_(i=1)^d x_i(1-x_i),
  quad b_2(x):=b_1(x)^2.
$
对线弹性取
$
  bold(u)_N=b_1 hat(bold(u))_N,
$
则每个分量都具有零边界迹，故 $bold(u)_N in H_0^1(Omega;RR^d)$。对固支板取
$
  w_N=b_2 hat(w)_N,
$
则 $w_N$ 及其一阶导数在每个边界面上都为零，故 $w_N in H_0^2(Omega)$。

只要乘积具有所需 Sobolev 正则性，这些函数已经位于受约束空间。因此原来的连续残差等价定理可以逐字应用。边界截断不会破坏残差等价性，也不需要添加罚参数。

对一般区域，必须先构造具有相应消失阶数且全局足够光滑的边界定义函数。直接使用到边界的距离函数不能完成这一任务，因为距离函数通常在内部骨架处不光滑。这个几何构造问题与下面的商函数正则性问题彼此独立。

== 近似对象已经变成商函数

设原始字典空间为
$
  V_N=span{rho_k(bold(omega)_j dot x+c_j):1<=j<=N}.
$
乘截断后实际使用的空间为
$
  b_m V_N
  =span{b_m(x) rho_k(bold(omega)_j dot x+c_j):1<=j<=N}.
$
它不是原来的全局 $rho_k$ 岭函数空间。要用原字典定理分析 $b_m V_N$ 对物理解 $v^star$ 的逼近，实际需要逼近
$
  g^star:=v^star/b_m.
$
乘法算子的连续性只给出
$
  norm(v^star-b_m g_N)_(H^m)
  =norm(b_m(g^star-g_N))_(H^m)
  <=C_b norm(g^star-g_N)_(H^m).
$
因此，决定逼近率的是 $g^star$ 的 Sobolev 正则性和字典表示范数，而不是仅仅 $v^star$ 的正则性。

#proposition(title: [原 Sobolev 假设不蕴含商函数假设])[
  即使 $b_m$ 是多项式且无限光滑，也不存在一般连续映射
  $
    v in H^s(Omega) sect H_0^m(Omega)
    mapsto v/b_m in H^s(Omega).
  $
  因此，$v^star in H^s sect H_0^m$ 不能推出 $v^star/b_m in H^s$。
]

#proof[
  在平直边界附近取法向坐标 $x>=0$ 和光滑切向截断函数 $chi$。对 $m=1$，令
  $
    v(x,y)=x^alpha chi(x,y),
  $
  并在 $s-1/2<alpha<=s+1/2$ 中选取非整数 $alpha$。则 $v$ 可以属于 $H^s$ 且具有零迹，但 $v/x=x^(alpha-1)chi$ 不属于同一个 $H^s$。对 $m=2$，在
  $s-1/2<alpha<=s+3/2$ 中选取非整数 $alpha$，则可以有 $v in H^s sect H_0^2$，而 $v/x^2$ 不属于 $H^s$。局部反例乘内部截断后即可嵌入有界区域。
]

这个反例也回答了“边界函数足够光滑是否会使商函数假设自动成立”：不会。截断函数 $b_m$ 光滑只保证乘法 $g mapsto b_m g$ 连续；除法在 $b_m=0$ 的边界处是奇异操作。齐次边界数据本身即使无限光滑，也只规定有限阶迹为零，并不控制精确解靠近边界时的全部高阶消失结构。区域角点、系数跳跃和载荷相容性不足都会使商函数正则性低于物理解正则性。

== 何时商函数假设成立

在光滑盒状区域上，如果精确解在闭包上 $C^infinity$，并在每个边界面上以所需阶数消失，那么商函数也光滑。其基本原因是 Hadamard 型因子分解。例如在边界 $x=0$ 附近，若 $u(0,y)=0$，则
$
  u(x,y)=x integral_0^1 partial_x u(t x,y) dif t.
$
若板挠度还满足 $partial_x w(0,y)=0$，则
$
  w(x,y)=x^2 integral_0^1 (1-t) partial_(x x) w(t x,y) dif t.
$
对盒子的各个边界面依次分解，可分别得到 $u=b_1 g_u$ 与 $w=b_2 g_w$，其中 $g_u,g_w in C^infinity(overline(Omega))$。

这是一个充分条件，不是由“边界数据光滑”自动推出的结论。它要求精确解本身直到边界都具有足够正则性，并满足对应消失阶数；这必须由方程、区域、系数和数据的整体正则性共同保证，或对给定精确解直接验证。

== 加入商函数假设后可以恢复的结论

若明确增加
$
  g_u:=bold(u)^star/b_1 in H^(s_u)(Omega;RR^d),
  quad g_w:=w^star/b_2 in H^(s_w)(Omega),
$
并且相应字典表示范数有统一上界，则可以对 $g_u,g_w$ 应用原字典逼近定理。典型的 Sobolev 误差变为
$
  inf_(hat(bold(u))_N in V_N)
  norm(bold(u)^star-b_1 hat(bold(u))_N)_(H^1)
  lt.tilde N^(-(s_u-1)/d) norm(g_u)_(H^(s_u)),
$
以及二维板上的
$
  inf_(hat(w)_N in V_N)
  norm(w^star-b_2 hat(w)_N)_(H^2)
  lt.tilde N^(-(s_w-2)/2) norm(g_w)_(H^(s_w)),
$
其中 $s_u,s_w$ 仍需位于所用字典定理的有效正则性范围内。关于 $N$ 的指数可以与原速率相同，但误差常数依赖商函数而非原物理解的同阶范数。这不是记号变化，而是严格更强的解假设。

固定光滑截断还具有一个计算优势：$b_m rho_k(bold(omega) dot x+c)$ 仍然对输出层系数线性，且不存在辅助 Ritz 维数 $K$。在截断函数及其前 $m$ 阶导数有界、字典参数域和输出系数预算固定时，内部残差的包络与复杂度仍可由相同方法控制，只需把乘积求导产生的有界系数计入常数。于是训练误差分析不再包含投影矩阵的 $K$ 截断项与投影积分项。

不过，这一简化只有在商函数的逼近范数和输出系数预算得到统一控制后才成立。若 $v^star/b_m$ 在边界附近变大或失去正则性，所需系数预算会增长，复杂度常数也随之退化；不能只凭试探函数满足边界条件就删除这些依赖。

= 三种方案的严格比较

#figure(
  table(
    columns: (1.05fr, 1.25fr, 1.25fr, 1.25fr),
    align: (left, left, left, left),
    inset: 7pt,
    stroke: .5pt,
    [*判定项*], [*正交投影*], [*自然迹泛函残差*], [*固定边界截断*],
    [连续图范数等价],
    [原定理直接成立],
    [加入商迹范数后成立；普通边界 $L^2$ 不成立],
    [原定理直接成立],
    [原 Sobolev 假设下的逼近率],
    [无损保留],
    [无损保留],
    [不能推出],
    [需要新增的解假设],
    [不需要],
    [不需要],
    [$v^star/b_m$ 的同阶正则性与表示范数控制],
    [新增离散误差],
    [有限 Ritz 投影及其积分、求解误差],
    [分数阶迹 Gram 矩阵或非局部边界积分误差],
    [无投影误差；常数依赖截断函数],
    [能否沿用原点态经验误差证明],
    [可以，并加入投影实现项],
    [不能；必须分析非局部迹项],
    [可以，在商函数系数预算受控后调整包络常数],
  ),
  caption: [三种本质边界处理方式对现有分析链条的影响],
)

= 结论与选择原则

对三种方案可以给出不含模糊余地的选择结论。

1. 若要保持“受约束空间上的残差等价 $arrow.r$ 原 Sobolev 字典逼近率 $arrow.r$ 现有经验误差估计”这一整条证明链，而不增强精确解假设，应使用正交投影。有限维实现产生的误差可以作为独立项显式估计。

2. 若要把边界条件放入泛函，不能使用普通 $L^2(partial Omega)$ 罚项来声称 $H^1$ 或 $H^2$ 图范数等价。必须使用自然迹商范数。这样连续稳定性和确定性逼近率都成立，但非局部边界项的离散与泛化分析必须重新建立。

3. 若区域允许构造固定光滑截断，并且已经证明或直接验证 $v^star/b_m$ 具有所需同阶正则性与可控表示范数，可以使用边界截断。此时连续稳定性保留，投影辅助误差消失，关于字典规模的逼近指数也可恢复。

4. 若只有 $v^star in H^s sect H_0^m$，没有商函数正则性，则不能用边界截断替换投影后继续沿用原逼近结论。这个障碍是真实的函数空间障碍，不是证明技巧不足，也不会因截断函数或齐次边界数据足够光滑而自动消失。

综上，投影不是唯一数学正确的边界处理方式，但它是唯一在现有解正则性假设下同时保留连续残差等价、确定性逼近率和原有证明结构的方式。自然迹残差以更复杂的非局部计算换取无约束试探空间；边界截断以更强的商函数正则性换取更简单的离散实现。三者的差异应当在定理假设和误差分解中明确写出，不能仅凭数值实现形式互相替代。
